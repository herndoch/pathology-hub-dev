# Open Items and Next Steps

1. Implement tag-aware backend runtime.
2. Build/load approved-only SQLite tag index.
3. Add `tag_auto`, `tag_exact`, `tag_prefix`, `tag_browse` to `searchEvidence` while preserving default behavior.
4. Deploy/test/regression.
5. Update GPT Builder schema only after proof.
6. Optionally run/refactor v11 curriculum hardening.
7. Deepen figure cleanup if junk header/footer crops remain.
8. Separate lecture timestamp patch remains open.
