# Version Chronology and Artifact Map

## PathOut

1. Initial tagging → generated unmapped tags → consolidated architecture.
2. v2 sidecar/apply package.
3. v2.3 strict URL/slug promotion; API-live AP-diagnostic subset.
4. v10.5 governance treats PathOut AP-diagnostic tags as approved local curriculum tags.

## Textbooks

1. v2 repair filled missing primary tags.
2. v2.1 collapsed noisy page-number review tags.
3. v10.5 governance removes/hides generated `::Textbooks::` artifacts and cleans manifest summaries.

## Lectures

1. v10.1/v10.2 superseded.
2. v10.3 root-locked review packs and adjudication removed unmapped from 42,069 records.
3. v10.5 governance removes/hides `::Lectures::` artifact ontology tags.

## WHO

User confirmed tag-bearing rows are in `gs://pathology-hub-0/WHO/WHO_JSON_PROCESSED/*.json`. v11 design fuzzy-matches WHO tags to ABPath and auto-accepts score ≥ 90.

## Governed cleanup

v10.4 implemented governance/inheritance and promoted live metadata. v10.5 cleaned manifest summaries, applied stricter figure map filtering, and passed final authenticated API proof after key fix.

## Future / not live

v11 curriculum hardening and explicit tag-aware API modes are not proven live.
